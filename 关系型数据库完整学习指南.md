# 关系型数据库完整学习指南

> 面向全栈开发的 MySQL/PostgreSQL 学习文档,从基础到进阶
> 所有 SQL 代码均附详细中文注释

---

## 目录

- [一、数据库基础概念](#一数据库基础概念)
- [二、SQL 基础语法](#二sql-基础语法)
- [三、数据库设计](#三数据库设计)
- [四、进阶 SQL](#四进阶-sql)
- [五、索引与性能优化](#五索引与性能优化)
- [六、事务与并发控制](#六事务与并发控制)
- [七、Node.js 连接数据库](#七nodejs-连接数据库)
- [八、ORM 工具(Prisma)](#八orm-工具prisma)
- [九、常见面试题与陷阱](#九常见面试题与陷阱)

---

## 一、数据库基础概念

### 1.1 什么是数据库

**数据库(Database)**:有组织地存储数据的"仓库",可以高效地存、取、改、删数据。

**数据库管理系统(DBMS)**:管理数据库的软件,比如 MySQL、PostgreSQL、Oracle、SQL Server。

**SQL**:Structured Query Language,结构化查询语言,操作关系型数据库的标准语言。

### 1.2 关系型 vs 非关系型

| 类型 | 代表 | 特点 | 适用场景 |
|------|------|------|----------|
| 关系型(SQL) | MySQL、PostgreSQL、Oracle | 数据以表的形式存储,有严格的结构和关系 | 订单、用户、金融等需要事务一致性的场景 |
| 非关系型(NoSQL) | Redis、MongoDB、Elasticsearch | 灵活的数据结构(键值、文档、图等) | 缓存、日志、搜索、海量数据 |

**全栈开发的标配**:MySQL/PostgreSQL(主数据库)+ Redis(缓存)。

### 1.3 核心概念

- **数据库(Database)**:一个数据库可以包含多张表
- **表(Table)**:数据的二维结构,类似 Excel 的工作表
- **行(Row / Record)**:一条数据记录
- **列(Column / Field)**:数据的某个属性
- **主键(Primary Key)**:唯一标识一行数据的字段
- **外键(Foreign Key)**:指向另一张表主键的字段,用于建立表之间的关联

### 1.4 MySQL vs PostgreSQL 怎么选?

- **MySQL**:学习资源多,中小型项目首选,互联网公司用得多
- **PostgreSQL**:功能更强大,支持 JSON、地理位置等高级特性,大厂和需要复杂查询的项目用得多

**建议**:先学 MySQL,语法掌握后切换到 PostgreSQL 几乎无成本(90% 语法相通)。

---

## 二、SQL 基础语法

### 2.1 数据类型

```sql
-- 整数类型
TINYINT      -- 1字节,范围 -128~127,常用于布尔值(0/1)或小数字(如年龄)
INT          -- 4字节,范围约 ±21亿,最常用的整数类型
BIGINT       -- 8字节,大数值用,比如用户ID、订单ID(防止数据量大时溢出)

-- 小数类型
DECIMAL(10,2)  -- 精确小数,10位数字其中2位小数,金额必须用这个!
FLOAT          -- 单精度浮点数,有精度丢失,不要用于金额
DOUBLE         -- 双精度浮点数,同上,不要用于金额

-- 字符串类型
CHAR(10)       -- 固定长度字符串,不够会用空格填充,适合固定长度(如手机号)
VARCHAR(100)   -- 可变长度字符串,最常用,长度根据实际内容
TEXT           -- 大文本,适合文章、评论等长文本

-- 日期时间类型
DATE           -- 日期 2024-01-01
TIME           -- 时间 12:30:00
DATETIME       -- 日期+时间 2024-01-01 12:30:00,范围大
TIMESTAMP      -- 时间戳,会自动转时区,推荐用于 created_at/updated_at

-- 其他
BOOLEAN        -- 布尔值(MySQL中实际是TINYINT(1))
JSON           -- JSON格式数据(MySQL 5.7+ 支持)
```

### 2.2 创建数据库和表

```sql
-- 创建数据库
-- CHARACTER SET utf8mb4 支持emoji和所有Unicode字符,务必用这个,不要用utf8(不完整的utf8)
-- COLLATE 是排序规则,utf8mb4_unicode_ci 是大小写不敏感的Unicode排序
CREATE DATABASE my_shop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 切换到该数据库
USE my_shop;

-- 创建一张用户表
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,        -- 主键,自动递增
    username VARCHAR(50) NOT NULL,            -- 用户名,不允许为空
    email VARCHAR(100) NOT NULL UNIQUE,       -- 邮箱,唯一约束(不能重复)
    age TINYINT,                              -- 年龄,可以为空
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP  -- 创建时间,默认当前时间
);

-- 删除表(危险操作,会丢失所有数据)
DROP TABLE users;

-- 修改表结构
ALTER TABLE users ADD COLUMN phone VARCHAR(20);     -- 添加新列
ALTER TABLE users DROP COLUMN phone;                -- 删除列
ALTER TABLE users MODIFY age INT;                   -- 修改列类型
ALTER TABLE users RENAME COLUMN username TO name;   -- 重命名列
```

### 2.3 增删改查(CRUD)

#### INSERT 插入数据

```sql
-- 插入单条数据
-- VALUES 后面括号里的值要和前面字段顺序一一对应
INSERT INTO users (username, email, age)
VALUES ('张三', 'zhangsan@example.com', 25);

-- 插入多条数据(性能比循环单条插入高得多)
INSERT INTO users (username, email, age) VALUES
    ('李四', 'lisi@example.com', 30),
    ('王五', 'wangwu@example.com', 28),
    ('赵六', 'zhaoliu@example.com', 35);

-- 不写字段名时,必须按表字段顺序插入所有值(不推荐,表结构变了会出错)
INSERT INTO users VALUES (NULL, '钱七', 'qianqi@example.com', 22, NOW());
```

#### SELECT 查询数据

```sql
-- 查询所有列(* 表示所有字段,生产环境不推荐用,会查出不必要的字段)
SELECT * FROM users;

-- 查询指定列(推荐!只取需要的字段,性能更好)
SELECT id, username, email FROM users;

-- 给列起别名(用 AS,也可以省略 AS)
SELECT username AS 用户名, email AS 邮箱 FROM users;

-- WHERE 条件过滤
SELECT * FROM users WHERE age > 25;                    -- 年龄大于25
SELECT * FROM users WHERE age >= 25 AND age <= 30;     -- 年龄在25-30之间
SELECT * FROM users WHERE age BETWEEN 25 AND 30;       -- 同上,更简洁
SELECT * FROM users WHERE username = '张三';            -- 等于
SELECT * FROM users WHERE username != '张三';           -- 不等于(也可以用 <>)
SELECT * FROM users WHERE age IS NULL;                 -- 判断NULL必须用IS,不能用=
SELECT * FROM users WHERE age IS NOT NULL;             -- 非空

-- IN 在集合中
SELECT * FROM users WHERE id IN (1, 3, 5);             -- id 是 1、3 或 5

-- LIKE 模糊匹配
-- % 表示任意多个字符,_ 表示一个字符
SELECT * FROM users WHERE username LIKE '张%';          -- 以"张"开头
SELECT * FROM users WHERE email LIKE '%@gmail.com';    -- 以"@gmail.com"结尾
SELECT * FROM users WHERE username LIKE '_三';          -- 第二个字是"三"

-- ORDER BY 排序
SELECT * FROM users ORDER BY age ASC;     -- 按年龄升序(ASC默认可省略)
SELECT * FROM users ORDER BY age DESC;    -- 按年龄降序
SELECT * FROM users ORDER BY age DESC, id ASC;  -- 多字段排序:先按age降序,age相同时按id升序

-- LIMIT 分页(MySQL 语法)
SELECT * FROM users LIMIT 10;             -- 取前10条
SELECT * FROM users LIMIT 10 OFFSET 20;   -- 跳过前20条,取10条(第3页,每页10条)
SELECT * FROM users LIMIT 20, 10;         -- 同上,MySQL 简写,第一个是偏移量
```

#### UPDATE 更新数据

```sql
-- 更新数据(一定要带 WHERE!不带的话会更新整张表!)
UPDATE users SET age = 26 WHERE id = 1;

-- 同时更新多个字段
UPDATE users
SET age = 27, email = 'new@example.com'
WHERE id = 1;

-- 危险!没有 WHERE 会更新所有行!
-- UPDATE users SET age = 30;  -- 千万别这么干

-- 安全建议:开发环境先用 SELECT 验证 WHERE 条件
SELECT * FROM users WHERE id = 1;  -- 先确认要改的就是这条
UPDATE users SET age = 26 WHERE id = 1;
```

#### DELETE 删除数据

```sql
-- 删除单条(一样,一定要带 WHERE!)
DELETE FROM users WHERE id = 1;

-- 删除多条
DELETE FROM users WHERE age < 18;

-- 危险!没有 WHERE 会清空整张表!
-- DELETE FROM users;

-- TRUNCATE 清空表(比 DELETE 更快,但不能回滚,且会重置自增ID)
TRUNCATE TABLE users;

-- 软删除模式(推荐!实际开发常用)
-- 不真的删除,而是加一个 deleted_at 字段标记
-- 这样数据还在,可以恢复,适合大部分业务场景
ALTER TABLE users ADD COLUMN deleted_at TIMESTAMP NULL;
UPDATE users SET deleted_at = NOW() WHERE id = 1;  -- "删除"用户
SELECT * FROM users WHERE deleted_at IS NULL;       -- 查询未删除的用户
```

---

## 三、数据库设计

> 这是后端开发最核心的能力之一,设计好表结构比写好 SQL 更重要

### 3.1 三大范式

**第一范式(1NF)**:每个字段都不可再分

```sql
-- ❌ 不符合 1NF:地址字段塞了多个信息
CREATE TABLE bad_users (
    id INT,
    address VARCHAR(200)  -- "北京市朝阳区xx路1号" 这种混在一起
);

-- ✅ 符合 1NF:拆分成独立字段
CREATE TABLE good_users (
    id INT,
    province VARCHAR(20),   -- 省
    city VARCHAR(20),       -- 市
    district VARCHAR(20),   -- 区
    detail VARCHAR(100)     -- 详细地址
);
```

**第二范式(2NF)**:非主键字段必须完全依赖主键(消除部分依赖)

**第三范式(3NF)**:非主键字段不能依赖其他非主键字段(消除传递依赖)

```sql
-- ❌ 不符合 3NF:department_name 依赖于 department_id,而不是直接依赖主键 id
CREATE TABLE bad_employees (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    department_id INT,
    department_name VARCHAR(50)  -- 这个字段冗余了!应该单独建表
);

-- ✅ 符合 3NF:拆分成两张表
CREATE TABLE departments (
    id INT PRIMARY KEY,
    name VARCHAR(50)
);

CREATE TABLE employees (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    department_id INT,  -- 只存关联ID,通过JOIN获取部门名
    FOREIGN KEY (department_id) REFERENCES departments(id)
);
```

**实战提醒**:范式不是教条,有时为了性能会"反范式"(故意冗余字段),比如订单表存下单时的商品价格,因为商品价格会变,但订单的历史价格必须保留。

### 3.2 表关系设计

#### 一对一(1:1)
比如 用户表 和 用户详情表(分离常用字段和不常用字段)。

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL
);

CREATE TABLE user_profiles (
    user_id INT PRIMARY KEY,             -- 主键同时也是外键,保证一对一
    avatar VARCHAR(255),                  -- 头像
    bio TEXT,                             -- 个人简介
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### 一对多(1:N)
最常见的关系,比如 一个用户有多个订单。

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50)
);

CREATE TABLE orders (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,                 -- 外键放在"多"的一方
    total_amount DECIMAL(10, 2),
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### 多对多(M:N)
比如 学生 和 课程,一个学生选多门课,一门课多个学生选。需要**中间表**。

```sql
CREATE TABLE students (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50)
);

CREATE TABLE courses (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50)
);

-- 中间表:存储两边的关联关系
CREATE TABLE student_courses (
    student_id INT,
    course_id INT,
    enrolled_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- 可以加额外字段(选课时间、成绩等)
    PRIMARY KEY (student_id, course_id),              -- 联合主键,保证不重复选课
    FOREIGN KEY (student_id) REFERENCES students(id),
    FOREIGN KEY (course_id) REFERENCES courses(id)
);
```

### 3.3 实战:设计一个电商系统

```sql
-- 用户表
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,             -- 主键,BIGINT 防止数据量大时溢出
    username VARCHAR(50) NOT NULL UNIQUE,             -- 用户名,UNIQUE 保证唯一
    email VARCHAR(100) NOT NULL UNIQUE,               -- 邮箱
    password_hash VARCHAR(255) NOT NULL,              -- 密码哈希!绝对不能存明文!
    phone VARCHAR(20),                                -- 手机号(可为空,有些场景非必填)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,   -- 创建时间,自动填充
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    -- ON UPDATE CURRENT_TIMESTAMP 让这个字段在行被修改时自动更新
    deleted_at TIMESTAMP NULL                         -- 软删除标记
);

-- 商品分类表
CREATE TABLE categories (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    parent_id INT NULL,                               -- 父分类ID,实现树形结构(NULL表示根分类)
    FOREIGN KEY (parent_id) REFERENCES categories(id)
);

-- 商品表
CREATE TABLE products (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(200) NOT NULL,
    description TEXT,                                 -- 商品描述,可能很长用TEXT
    price DECIMAL(10, 2) NOT NULL,                    -- 价格!必须用DECIMAL,不要用FLOAT
    stock INT NOT NULL DEFAULT 0,                     -- 库存,默认0
    category_id INT,                                  -- 分类ID
    image_url VARCHAR(500),                           -- 主图URL
    status TINYINT DEFAULT 1,                         -- 状态:1上架 0下架,用TINYINT节省空间
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (category_id) REFERENCES categories(id)
);

-- 订单表
CREATE TABLE orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(32) NOT NULL UNIQUE,             -- 订单号,业务唯一标识(对外展示用)
    user_id BIGINT NOT NULL,
    total_amount DECIMAL(10, 2) NOT NULL,             -- 订单总金额
    status VARCHAR(20) DEFAULT 'pending',
    -- 订单状态:pending(待支付) / paid(已支付) / shipped(已发货) / completed(已完成) / cancelled(已取消)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    paid_at TIMESTAMP NULL,                           -- 支付时间(支付后才有值)
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 订单详情表(一个订单包含多个商品)
CREATE TABLE order_items (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_id BIGINT NOT NULL,
    product_id BIGINT NOT NULL,
    quantity INT NOT NULL,                            -- 购买数量
    unit_price DECIMAL(10, 2) NOT NULL,
    -- ⚠️ 重点:这里存下单时的单价,不能直接关联 products.price
    -- 因为商品价格会变,但订单的历史价格必须保留(类似快照)
    product_name VARCHAR(200) NOT NULL,               -- 同理,商品名也冗余存储
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (product_id) REFERENCES products(id)
);

-- 地址表(一个用户多个收货地址)
CREATE TABLE addresses (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    recipient VARCHAR(50) NOT NULL,                   -- 收件人姓名
    phone VARCHAR(20) NOT NULL,
    province VARCHAR(20),
    city VARCHAR(20),
    district VARCHAR(20),
    detail VARCHAR(200),
    is_default BOOLEAN DEFAULT FALSE,                 -- 是否默认地址
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### 3.4 设计原则总结

1. **主键用 BIGINT AUTO_INCREMENT**(防止数据量大时溢出)
2. **金额必须用 DECIMAL**,不要用 FLOAT/DOUBLE(浮点数精度问题)
3. **密码必须哈希存储**,绝不能存明文
4. **时间字段加上自动填充**(`DEFAULT CURRENT_TIMESTAMP` 和 `ON UPDATE`)
5. **状态字段优先用 TINYINT 或 VARCHAR**,不要用 ENUM(扩展性差)
6. **能用 NOT NULL 就用**,NULL 会让查询和索引变复杂
7. **冗余字段是允许的**,只要业务需要(如订单中的商品名、单价)
8. **历史数据要做快照**(订单价格、商品名等)
9. **大文本用 TEXT**,普通字符串用 VARCHAR
10. **生产环境推荐软删除**(加 deleted_at 字段)

---

## 四、进阶 SQL

### 4.1 聚合函数

```sql
-- 常用聚合函数
SELECT COUNT(*) FROM users;                  -- 统计行数(包含NULL)
SELECT COUNT(email) FROM users;              -- 统计email非NULL的行数
SELECT COUNT(DISTINCT age) FROM users;       -- 统计不重复的age数量

SELECT SUM(total_amount) FROM orders;        -- 求和
SELECT AVG(total_amount) FROM orders;        -- 平均值
SELECT MAX(total_amount) FROM orders;        -- 最大值
SELECT MIN(total_amount) FROM orders;        -- 最小值
```

### 4.2 GROUP BY 分组

```sql
-- 按用户分组,统计每个用户的订单数
SELECT user_id, COUNT(*) AS order_count
FROM orders
GROUP BY user_id;

-- 按用户分组,统计每个用户的总消费
SELECT user_id, SUM(total_amount) AS total_spent
FROM orders
GROUP BY user_id
ORDER BY total_spent DESC;                   -- 按总消费降序排列

-- HAVING:对分组后的结果进行过滤(WHERE不能用聚合函数)
-- 查询总消费超过1000的用户
SELECT user_id, SUM(total_amount) AS total_spent
FROM orders
GROUP BY user_id
HAVING total_spent > 1000;
-- 关键区别:WHERE 在分组前过滤行,HAVING 在分组后过滤组

-- 综合示例:查询2024年下单超过3次的用户及其总消费,按总消费排序
SELECT
    user_id,
    COUNT(*) AS order_count,
    SUM(total_amount) AS total_spent
FROM orders
WHERE created_at >= '2024-01-01'              -- 先过滤2024年的订单
  AND created_at < '2025-01-01'
GROUP BY user_id                              -- 按用户分组
HAVING COUNT(*) > 3                           -- 过滤分组后:订单数 > 3
ORDER BY total_spent DESC                     -- 按总消费降序
LIMIT 10;                                     -- 取前10名
```

**执行顺序(重要,面试常考)**:
```
FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT
```

### 4.3 JOIN 连接查询

JOIN 是 SQL 的灵魂,用于关联多张表查询。

#### INNER JOIN(内连接)

只返回两表中匹配的数据。

```sql
-- 查询所有订单及对应的用户名(只显示有用户的订单)
SELECT
    orders.id AS order_id,
    orders.total_amount,
    users.username
FROM orders
INNER JOIN users ON orders.user_id = users.id;
-- ON 后面是连接条件,通常是外键和主键的关联

-- 用表别名,更简洁(强烈推荐)
SELECT
    o.id AS order_id,
    o.total_amount,
    u.username
FROM orders o                                 -- orders 别名为 o
INNER JOIN users u ON o.user_id = u.id;       -- users 别名为 u
```

#### LEFT JOIN(左连接)

返回左表全部数据,右表没匹配的显示 NULL。

```sql
-- 查询所有用户及其订单数(包括没下过单的用户)
SELECT
    u.username,
    COUNT(o.id) AS order_count       -- 没订单的用户这里是 0
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
GROUP BY u.id, u.username;
-- 用 COUNT(o.id) 而不是 COUNT(*),因为没订单的用户 o.id 是 NULL,COUNT 会忽略 NULL
```

#### RIGHT JOIN(右连接)

返回右表全部数据,左表没匹配的显示 NULL。

```sql
-- 实际开发中很少用 RIGHT JOIN,通常都改写成 LEFT JOIN(把表顺序换一下)
SELECT u.username, o.id
FROM orders o
RIGHT JOIN users u ON o.user_id = u.id;
-- 等价于:
SELECT u.username, o.id
FROM users u
LEFT JOIN orders o ON o.user_id = u.id;
```

#### 三表关联

```sql
-- 查询订单详情:订单号、用户名、商品名、数量
SELECT
    o.order_no,
    u.username,
    oi.product_name,
    oi.quantity,
    oi.unit_price
FROM orders o
INNER JOIN users u ON o.user_id = u.id              -- 关联用户表
INNER JOIN order_items oi ON oi.order_id = o.id     -- 关联订单详情表
WHERE o.status = 'paid'                              -- 只查已支付的订单
ORDER BY o.created_at DESC;
```

#### JOIN 类型对比图

```
表 A          表 B
[1, 2, 3]     [2, 3, 4]

INNER JOIN  → [2, 3]           (两边都有)
LEFT JOIN   → [1, 2, 3]        (A 全部 + B 匹配的)
RIGHT JOIN  → [2, 3, 4]        (B 全部 + A 匹配的)
FULL JOIN   → [1, 2, 3, 4]     (两边全部,MySQL 不支持,PG 支持)
```

### 4.4 子查询

```sql
-- 子查询作为条件:查询消费金额超过平均值的订单
SELECT * FROM orders
WHERE total_amount > (SELECT AVG(total_amount) FROM orders);
-- 内层查询先算出平均值,外层查询用这个平均值过滤

-- 子查询作为表(派生表):查询每个用户的最高订单金额
SELECT user_id, MAX(total_amount) AS max_amount
FROM orders
GROUP BY user_id;

-- 把上面这个结果当成一张临时表,再关联用户表
SELECT u.username, t.max_amount
FROM users u
INNER JOIN (
    SELECT user_id, MAX(total_amount) AS max_amount
    FROM orders
    GROUP BY user_id
) t ON u.id = t.user_id;                         -- t 是子查询的别名

-- EXISTS / NOT EXISTS:判断子查询是否有结果
-- 查询有过订单的用户
SELECT * FROM users u
WHERE EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id);
-- 性能比 IN 更好(IN 会把子查询全部查出来,EXISTS 找到一条就返回)

-- 查询从未下过订单的用户
SELECT * FROM users u
WHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id);
```

### 4.5 CASE WHEN 条件判断

```sql
-- 类似编程语言的 if-else
SELECT
    username,
    age,
    CASE
        WHEN age < 18 THEN '未成年'
        WHEN age < 60 THEN '成年人'
        ELSE '老年人'
    END AS age_group                              -- 给计算结果起别名
FROM users;

-- 实战:统计不同状态的订单数量
SELECT
    COUNT(CASE WHEN status = 'pending' THEN 1 END) AS pending_count,
    COUNT(CASE WHEN status = 'paid' THEN 1 END) AS paid_count,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) AS completed_count
FROM orders;
-- 这种技巧叫"行转列",在数据分析中非常有用
```

### 4.6 UNION 合并结果

```sql
-- UNION:合并两个查询的结果,自动去重
SELECT username FROM users WHERE age < 18
UNION
SELECT username FROM users WHERE age > 60;

-- UNION ALL:不去重(性能更好,如果确定没重复用这个)
SELECT username FROM users WHERE age < 18
UNION ALL
SELECT username FROM users WHERE age > 60;
```

---

## 五、索引与性能优化

### 5.1 什么是索引

**索引**是数据库的"目录",可以大幅加速查询。原理类似书的目录,不用翻整本书。

**代价**:占用磁盘空间,降低增删改的速度(因为要维护索引)。

### 5.2 索引类型

```sql
-- 1. 主键索引(创建表时自动建立,最快)
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT
);

-- 2. 唯一索引(保证字段值唯一,同时也是索引)
CREATE TABLE users (
    email VARCHAR(100) UNIQUE                     -- 建表时声明
);
-- 或者建表后添加
CREATE UNIQUE INDEX idx_email ON users(email);

-- 3. 普通索引(单字段)
CREATE INDEX idx_username ON users(username);

-- 4. 联合索引(多字段)
CREATE INDEX idx_user_status ON orders(user_id, status);
-- 这个索引能加速:WHERE user_id = ? AND status = ?
-- 也能加速:WHERE user_id = ?
-- 但不能加速:WHERE status = ?(因为索引最左前缀原则)

-- 5. 全文索引(用于文本搜索,MySQL专用,实际开发中复杂搜索用Elasticsearch)
CREATE FULLTEXT INDEX idx_description ON products(description);

-- 查看表的所有索引
SHOW INDEX FROM users;

-- 删除索引
DROP INDEX idx_username ON users;
```

### 5.3 什么字段该建索引

✅ **该建**:
- 经常作为 WHERE 条件的字段
- JOIN 关联的字段(外键)
- ORDER BY 排序的字段
- GROUP BY 分组的字段

❌ **不该建**:
- 数据量很小的表(全表扫描更快)
- 重复值很多的字段(如性别,只有男/女)
- 经常更新的字段(维护成本高)
- 表中很少查询的字段

### 5.4 最左前缀原则(联合索引)

```sql
-- 创建联合索引(a, b, c)
CREATE INDEX idx_abc ON orders(user_id, status, created_at);

-- ✅ 能用上索引的查询:
SELECT * FROM orders WHERE user_id = 1;
SELECT * FROM orders WHERE user_id = 1 AND status = 'paid';
SELECT * FROM orders WHERE user_id = 1 AND status = 'paid' AND created_at > '2024-01-01';

-- ❌ 不能用上索引的查询(违反最左前缀):
SELECT * FROM orders WHERE status = 'paid';                      -- 跳过了 user_id
SELECT * FROM orders WHERE created_at > '2024-01-01';            -- 跳过了前面两个
SELECT * FROM orders WHERE user_id = 1 AND created_at > '2024-01-01'; -- 中间跳过了 status,只能用 user_id 部分
```

**记忆方法**:联合索引像火车,只能从车头开始连续使用,不能跳过中间的车厢。

### 5.5 EXPLAIN 分析查询性能

```sql
-- 在 SELECT 前加 EXPLAIN,可以看到查询的执行计划
EXPLAIN SELECT * FROM orders WHERE user_id = 1;

-- 关键字段含义:
-- type:连接类型,性能从好到差:
--   const  - 通过主键/唯一索引查找,最快
--   ref    - 通过普通索引查找
--   range  - 范围查找
--   index  - 全索引扫描
--   ALL    - 全表扫描!性能最差,要优化!
--
-- key:实际用到的索引(NULL 表示没用索引)
-- rows:预估扫描的行数,越小越好
-- Extra:额外信息
--   Using index    - 覆盖索引,很好
--   Using where    - 用了where条件
--   Using filesort - 需要额外排序,性能差,考虑加索引
--   Using temporary - 用了临时表,性能差
```

### 5.6 常见性能优化技巧

```sql
-- 1. 避免 SELECT *,只查需要的字段
-- ❌ 慢
SELECT * FROM users WHERE id = 1;
-- ✅ 快
SELECT id, username, email FROM users WHERE id = 1;

-- 2. WHERE 条件不要对字段做运算(会让索引失效)
-- ❌ 索引失效
SELECT * FROM users WHERE YEAR(created_at) = 2024;
-- ✅ 索引生效
SELECT * FROM users WHERE created_at >= '2024-01-01' AND created_at < '2025-01-01';

-- 3. LIKE 通配符在左边索引失效
-- ❌ 索引失效
SELECT * FROM users WHERE username LIKE '%张';
-- ✅ 索引生效
SELECT * FROM users WHERE username LIKE '张%';

-- 4. 大数据量分页优化
-- ❌ 慢:LIMIT 100000, 10 会扫描 100010 行
SELECT * FROM orders ORDER BY id LIMIT 100000, 10;
-- ✅ 快:用上次查到的最大id作为起点
SELECT * FROM orders WHERE id > 100000 ORDER BY id LIMIT 10;

-- 5. 避免 OR(可能导致索引失效),用 UNION 替代
-- ❌ 可能慢
SELECT * FROM users WHERE username = '张三' OR email = 'a@b.com';
-- ✅ 可能更快
SELECT * FROM users WHERE username = '张三'
UNION
SELECT * FROM users WHERE email = 'a@b.com';

-- 6. 用 LIMIT 限制结果集
-- 如果只需要一条,加 LIMIT 1 可以让数据库找到就停止
SELECT * FROM users WHERE email = 'a@b.com' LIMIT 1;
```

---

## 六、事务与并发控制

### 6.1 什么是事务

**事务**:一组操作,要么全部成功,要么全部失败。

**经典例子**:银行转账,A 减 100 元,B 加 100 元,这两步必须同时成功或同时失败,不能 A 减了但 B 没加。

### 6.2 ACID 特性

- **A(Atomicity)原子性**:事务中的操作要么全成功要么全失败,不能只执行一部分
- **C(Consistency)一致性**:事务前后,数据满足业务规则(比如转账前后总金额不变)
- **I(Isolation)隔离性**:多个事务并发时不互相干扰
- **D(Durability)持久性**:事务一旦提交,数据永久保存(就算断电也不丢)

### 6.3 事务语法

```sql
-- 开始事务
START TRANSACTION;  -- 或者 BEGIN;

-- 执行一系列操作:转账例子
UPDATE accounts SET balance = balance - 100 WHERE id = 1;  -- A扣100
UPDATE accounts SET balance = balance + 100 WHERE id = 2;  -- B加100

-- 检查没问题就提交
COMMIT;

-- 出问题就回滚(所有操作撤销)
-- ROLLBACK;

-- 完整的转账示例(伪代码逻辑)
START TRANSACTION;

-- 1. 检查A的余额是否足够
SELECT balance FROM accounts WHERE id = 1;
-- 假设余额是 500,够扣

-- 2. 扣A的钱
UPDATE accounts SET balance = balance - 100 WHERE id = 1;

-- 3. 加B的钱
UPDATE accounts SET balance = balance + 100 WHERE id = 2;

-- 4. 都成功了,提交
COMMIT;

-- 如果第3步失败,执行:
-- ROLLBACK;  -- 第2步的扣款也会撤销
```

### 6.4 隔离级别

并发事务可能出现的问题:

- **脏读**:事务A读到了事务B未提交的数据
- **不可重复读**:同一事务内两次读取同一数据,结果不一样(因为其他事务修改了)
- **幻读**:同一事务内两次查询,出现了新行(因为其他事务插入了)

四种隔离级别(从低到高):

| 隔离级别 | 脏读 | 不可重复读 | 幻读 | 性能 |
|---------|------|-----------|------|------|
| READ UNCOMMITTED 读未提交 | ❌可能 | ❌可能 | ❌可能 | 最好 |
| READ COMMITTED 读已提交 | ✅避免 | ❌可能 | ❌可能 | 好 |
| REPEATABLE READ 可重复读(MySQL默认) | ✅避免 | ✅避免 | ❌可能 | 中 |
| SERIALIZABLE 串行化 | ✅避免 | ✅避免 | ✅避免 | 最差 |

```sql
-- 查看当前隔离级别
SELECT @@transaction_isolation;

-- 设置当前会话的隔离级别
SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;
```

**实战建议**:大部分场景用默认的 REPEATABLE READ 就行,不要瞎改。

### 6.5 锁

```sql
-- 悲观锁:查询时就锁住,防止其他事务修改
-- 适合冲突频繁的场景,如秒杀
START TRANSACTION;
SELECT * FROM products WHERE id = 1 FOR UPDATE;  -- 排他锁,其他事务不能修改这行
UPDATE products SET stock = stock - 1 WHERE id = 1;
COMMIT;

-- 共享锁:允许其他事务读,不允许写
SELECT * FROM products WHERE id = 1 LOCK IN SHARE MODE;

-- 乐观锁:用版本号实现,不依赖数据库锁
-- 表中加一个 version 字段
UPDATE products
SET stock = stock - 1, version = version + 1
WHERE id = 1 AND version = 5;
-- 如果 version 已经被其他事务改了,这条UPDATE影响0行,业务层判断重试
```

---

## 七、Node.js 连接数据库

### 7.1 安装驱动

```bash
# MySQL
npm install mysql2

# PostgreSQL
npm install pg
```

### 7.2 基础用法(MySQL 示例)

```javascript
// 引入 mysql2 的 promise 版本,支持 async/await(强烈推荐)
const mysql = require('mysql2/promise');

// 创建连接池(生产环境必须用连接池,不要每次都创建连接)
// 连接池会复用连接,避免频繁建立连接的开销
const pool = mysql.createPool({
    host: 'localhost',           // 数据库地址
    user: 'root',                // 用户名
    password: 'your_password',   // 密码
    database: 'my_shop',         // 数据库名
    waitForConnections: true,    // 连接池满了是否等待
    connectionLimit: 10,         // 最大连接数,根据业务调整
    queueLimit: 0                // 等待队列长度,0表示无限
});

// 查询示例
async function getUsers() {
    try {
        // execute 比 query 更安全,自动处理SQL注入
        // 返回值是数组:[结果行, 字段元信息]
        const [rows, fields] = await pool.execute(
            'SELECT id, username, email FROM users WHERE age > ?',
            [18]  // 参数化查询!防止SQL注入!
        );
        console.log(rows);
        return rows;
    } catch (error) {
        console.error('查询失败:', error);
        throw error;
    }
}

// 插入示例
async function createUser(username, email, age) {
    const [result] = await pool.execute(
        'INSERT INTO users (username, email, age) VALUES (?, ?, ?)',
        [username, email, age]
    );
    // result.insertId 是新插入行的自增ID
    // result.affectedRows 是影响的行数
    return result.insertId;
}

// 更新示例
async function updateUserAge(id, age) {
    const [result] = await pool.execute(
        'UPDATE users SET age = ? WHERE id = ?',
        [age, id]
    );
    return result.affectedRows;  // 返回受影响的行数
}

// 事务示例(转账)
async function transfer(fromId, toId, amount) {
    // 从连接池获取一个连接(事务必须在同一个连接上执行)
    const conn = await pool.getConnection();
    try {
        await conn.beginTransaction();                // 开启事务

        // 1. 扣款
        const [r1] = await conn.execute(
            'UPDATE accounts SET balance = balance - ? WHERE id = ? AND balance >= ?',
            [amount, fromId, amount]
        );
        if (r1.affectedRows === 0) {
            throw new Error('余额不足或账户不存在');
        }

        // 2. 加款
        await conn.execute(
            'UPDATE accounts SET balance = balance + ? WHERE id = ?',
            [amount, toId]
        );

        await conn.commit();                          // 都成功,提交事务
        console.log('转账成功');
    } catch (error) {
        await conn.rollback();                        // 出错回滚
        console.error('转账失败:', error);
        throw error;
    } finally {
        conn.release();                               // 务必释放连接回连接池!
    }
}
```

### 7.3 SQL 注入防护

```javascript
// ❌ 极度危险!SQL 注入漏洞!
const username = req.body.username;  // 用户输入
const sql = `SELECT * FROM users WHERE username = '${username}'`;
// 如果用户输入: ' OR '1'='1
// SQL 会变成: SELECT * FROM users WHERE username = '' OR '1'='1'
// 后果:查出所有用户!甚至可能被删表!

// ✅ 安全:使用参数化查询
const [rows] = await pool.execute(
    'SELECT * FROM users WHERE username = ?',  // ? 是占位符
    [username]                                  // 参数单独传入
);
// 数据库驱动会自动转义特殊字符,杜绝注入
```

---

## 八、ORM 工具(Prisma)

ORM(Object-Relational Mapping)对象关系映射,让你用对象操作数据库,不用写 SQL。

**Prisma** 是 Node.js 生态最流行的现代 ORM,类型安全、文档好、配合 TypeScript 完美。

### 8.1 安装与初始化

```bash
npm install prisma --save-dev
npm install @prisma/client
npx prisma init                    # 初始化 prisma 目录和 .env 文件
```

### 8.2 定义数据模型(schema.prisma)

```prisma
// prisma/schema.prisma
generator client {
    provider = "prisma-client-js"   // 生成 JS 客户端
}

datasource db {
    provider = "mysql"               // 数据库类型
    url      = env("DATABASE_URL")   // 连接字符串从 .env 读取
}

// 定义用户模型(对应数据库的 users 表)
model User {
    id        Int      @id @default(autoincrement())  // 主键,自增
    username  String   @unique                        // 用户名,唯一
    email     String   @unique
    age       Int?                                    // ? 表示可为空
    orders    Order[]                                 // 一对多关系:一个用户多个订单
    createdAt DateTime @default(now())                // 默认值是当前时间
    updatedAt DateTime @updatedAt                     // 自动更新
}

model Order {
    id          Int      @id @default(autoincrement())
    totalAmount Decimal  @db.Decimal(10, 2)           // 指定数据库类型
    status      String   @default("pending")
    userId      Int                                   // 外键字段
    user        User     @relation(fields: [userId], references: [id])  // 定义关联
    createdAt   DateTime @default(now())
}
```

### 8.3 生成数据库表并使用

```bash
# 根据 schema 生成数据库表(开发环境)
npx prisma migrate dev --name init

# 生成类型安全的客户端
npx prisma generate
```

```javascript
// 使用 Prisma 操作数据库(注意:全自动类型推断,TS下体验极好)
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();

// 创建用户
const user = await prisma.user.create({
    data: {
        username: '张三',
        email: 'zhangsan@example.com',
        age: 25
    }
});

// 查询所有用户
const users = await prisma.user.findMany();

// 条件查询
const adults = await prisma.user.findMany({
    where: { age: { gte: 18 } }     // gte = greater than or equal,大于等于
});

// 查询单个(根据唯一字段)
const user2 = await prisma.user.findUnique({
    where: { email: 'zhangsan@example.com' }
});

// 关联查询(自动 JOIN)
const userWithOrders = await prisma.user.findUnique({
    where: { id: 1 },
    include: { orders: true }       // 同时查出这个用户的所有订单
});

// 更新
await prisma.user.update({
    where: { id: 1 },
    data: { age: 26 }
});

// 删除
await prisma.user.delete({
    where: { id: 1 }
});

// 事务(Prisma 提供的便捷写法)
await prisma.$transaction(async (tx) => {
    await tx.account.update({
        where: { id: 1 },
        data: { balance: { decrement: 100 } }
    });
    await tx.account.update({
        where: { id: 2 },
        data: { balance: { increment: 100 } }
    });
});
// 任一步骤失败,整个事务自动回滚
```

### 8.4 ORM vs 原生 SQL

| 维度 | ORM(Prisma) | 原生 SQL |
|------|--------------|---------|
| 开发速度 | 快 | 慢 |
| 学习成本 | 低 | 中(需要 SQL 基础) |
| 类型安全 | ✅(TS) | ❌ |
| 性能 | 中(有抽象层开销) | 高 |
| 复杂查询 | 受限 | 灵活 |
| 适合场景 | 90% 的常规 CRUD | 复杂报表、性能敏感的场景 |

**建议**:用 ORM 做常规增删改查,复杂统计/性能敏感处用原生 SQL(Prisma 也支持 `$queryRaw`)。

---

## 九、常见面试题与陷阱

### 9.1 高频面试题

**Q1: 主键和唯一键的区别?**
- 主键:不能为 NULL,一张表只能一个,自动创建聚簇索引
- 唯一键:可以为 NULL(且 NULL 不算重复),一张表可以多个

**Q2: char 和 varchar 的区别?**
- char(n):固定长度,不够用空格补,查询略快,适合长度固定的字段(手机号、UUID)
- varchar(n):可变长度,节省空间,适合长度变化大的字段(用户名、邮箱)

**Q3: DELETE、TRUNCATE、DROP 的区别?**
- DELETE:删数据,可加 WHERE,可回滚,慢,不重置自增ID
- TRUNCATE:清空表,不能加 WHERE,不能回滚,快,重置自增ID
- DROP:删除整张表(包括结构),不能回滚

**Q4: 为什么不建议用 SELECT *?**
- 查出不需要的字段,浪费网络和内存
- 可能用不上覆盖索引(回表查询)
- 表结构变化时容易出bug

**Q5: 索引为什么能加速查询?用的什么数据结构?**
- MySQL InnoDB 用的是 B+ 树
- B+ 树高度低(3-4层就能存千万数据),磁盘 IO 次数少
- 叶子节点形成链表,范围查询快

**Q6: 什么是回表?**
- 通过普通索引查到主键,再用主键查完整数据,这个过程叫"回表"
- 覆盖索引:索引包含了所有查询字段,不用回表,性能更好

### 9.2 常见陷阱

```sql
-- 陷阱1:NULL 不等于任何值,包括它自己
SELECT * FROM users WHERE age = NULL;       -- ❌ 永远查不到结果
SELECT * FROM users WHERE age IS NULL;      -- ✅ 正确写法

-- 陷阱2:COUNT(*) vs COUNT(字段)
SELECT COUNT(*) FROM users;                 -- 统计所有行
SELECT COUNT(age) FROM users;               -- 只统计 age 不为 NULL 的行!

-- 陷阱3:GROUP BY 字段必须出现在 SELECT 中(或用聚合函数)
-- ❌ MySQL 5.7+ 严格模式会报错
SELECT username, age, COUNT(*) FROM users GROUP BY age;
-- ✅ 正确
SELECT age, COUNT(*) FROM users GROUP BY age;

-- 陷阱4:LIMIT 大偏移量慢
-- ❌ 慢
SELECT * FROM orders ORDER BY id LIMIT 1000000, 10;
-- ✅ 用游标(基于上次查到的id)
SELECT * FROM orders WHERE id > 1000000 ORDER BY id LIMIT 10;

-- 陷阱5:字符串和数字比较会导致索引失效
-- 如果 phone 字段是 VARCHAR 类型:
SELECT * FROM users WHERE phone = 13800138000;     -- ❌ 索引失效
SELECT * FROM users WHERE phone = '13800138000';   -- ✅ 索引生效

-- 陷阱6:UPDATE/DELETE 忘记加 WHERE
-- 一定要先用 SELECT 验证条件!
SELECT * FROM users WHERE id = 1;          -- 先看看
UPDATE users SET age = 26 WHERE id = 1;    -- 再操作
```

### 9.3 全栈开发的数据库实战经验

1. **永远用参数化查询**,杜绝 SQL 注入
2. **生产环境用连接池**,不要每次请求都创建连接
3. **密码哈希后存储**,推荐 bcrypt
4. **金额用 DECIMAL**,不要用浮点数
5. **时间字段加索引**,提升按时间查询的性能
6. **大表分页用游标**,不要用 OFFSET
7. **频繁查询的字段加索引**,但不要滥用
8. **写慢查询时用 EXPLAIN 分析**,看看走没走索引
9. **重要操作用事务**,保证数据一致性
10. **数据库密码、连接串放在环境变量**,不要写死在代码里

---

## 附录:推荐学习资源

- **官方文档**:MySQL [dev.mysql.com/doc](https://dev.mysql.com/doc/),PostgreSQL [postgresql.org/docs](https://www.postgresql.org/docs/)
- **在线练习**:[sqlzoo.net](https://sqlzoo.net/)、LeetCode 数据库题库
- **可视化工具**:Navicat、DBeaver(免费)、TablePlus
- **Prisma 文档**:[prisma.io/docs](https://www.prisma.io/docs)

---

## 下一步学习方向

学完这份文档后,可以继续:

1. **Redis**:缓存、分布式锁、消息队列
2. **数据库设计模式**:分库分表、读写分离、主从复制
3. **MongoDB**:文档型数据库,适合灵活的数据结构
4. **数据库高可用**:主从复制、集群、备份恢复
5. **后端框架**:Express / NestJS / Koa 配合数据库实战

加油!数据库是后端最核心的技能之一,扎实掌握能让你的全栈能力上一个台阶。
