# 项目文件说明 & 企业开发规范

## 一、你现在有哪些文件（每个文件的作用）

### 📁 根目录 `postgres-learning/`

| 文件/文件夹 | 作用 |
|---|---|
| `docker-compose.yml` | Docker 的"配置清单"，定义了启动哪些容器（PostgreSQL、pgAdmin）、用什么端口、数据存哪里。一行命令 `docker compose up -d` 就能把整个数据库环境跑起来 |
| `backups/` | 存放数据库备份文件的文件夹（目前是空的，阶段5会用到） |
| `init-scripts/` | 数据库初始化脚本，容器**第一次启动**时自动执行里面的 SQL 文件 |
| `init-scripts/01-init.sql` | 创建 users、products、orders 三张表，并插入测试数据 |
| `init-scripts/02-jobs.sql` | 创建 jobs、amazon_sales 两张表 |

---

### 📁 `node-app/`（Node.js 项目）

#### 🔧 配置文件

| 文件 | 作用 |
|---|---|
| `.env` | 环境变量配置文件。存数据库密码、端口、连接地址等**敏感信息**。不提交到 Git，每个环境（开发/生产）有自己的版本 |
| `.gitignore` | 告诉 Git 哪些文件不要提交，比如 `node_modules/`、`.env`（包含密码） |
| `package.json` | Node.js 项目的"身份证"。记录项目名称、版本、依赖的第三方库、启动命令 |
| `package-lock.json` | 锁定每个依赖库的精确版本号，保证团队所有人安装的库版本完全一致，自动生成不要手动改 |

#### 🗄️ 数据库相关

| 文件 | 作用 |
|---|---|
| `db.js` | **连接池模块**。整个项目共用一个数据库连接池，所有其他文件都 `require('./db')` 来用。连接池能复用连接，避免每次请求都新建连接（性能关键） |
| `userRepository.js` | **用户数据访问层**。封装了所有对 users 表的操作（查询、新增、更新、删除、分页）。业务代码不直接写 SQL，通过调用这里的方法来操作数据库 |
| `transaction.js` | **事务工具函数**。封装了 `BEGIN/COMMIT/ROLLBACK` 的逻辑，提供 `withTransaction()` 方法，保证多个数据库操作要么全成功要么全回滚 |

#### 🌐 服务器

| 文件 | 作用 |
|---|---|
| `server.js` | **HTTP 服务器**。用 Express 框架搭建，定义了所有 API 接口（`GET /api/users`、`POST /api/orders` 等），是前端和数据库之间的桥梁 |

#### 🕷️ 爬虫

| 文件 | 作用 |
|---|---|
| `job-scraper.js` | **职位爬虫脚本**。模拟从 Boss直聘 抓取职位数据，清洗数据（解析薪资），然后存入 jobs 表 |
| `reload-jobs.js` | **数据重置脚本**。清空 jobs 表后重新插入中文测试数据，用于开发调试 |
| `insert-sales.js` | **数据插入脚本**。往 amazon_sales 表插入测试数据 |

#### 🧪 学习练习文件

| 文件 | 作用 |
|---|---|
| `01-simple-connect.js` | 最基础的数据库连接示例，用 Client（单连接）连接数据库并查询 |
| `02-parameterized-query.js` | 演示参数化查询（防 SQL 注入），用 `$1 $2` 占位符代替字符串拼接 |
| `03-test-pool.js` | 测试连接池，模拟 10 个并发查询，验证连接池的工作方式 |
| `04-test-crud.js` | 测试 userRepository.js 的所有增删改查方法 |
| `05-transaction.js` | 测试事务：下单场景（检查库存→创建订单→扣减库存），演示 ROLLBACK 回滚 |
| `07-prisma-test.js` | 测试 Prisma ORM 的各种功能（查询、关联、事务、聚合统计） |
| `test-direct.js` | 调试用的临时文件，硬编码连接配置排查连接问题（可以删掉） |

#### 🤖 Prisma ORM

| 文件/文件夹 | 作用 |
|---|---|
| `prisma/schema.prisma` | Prisma 的数据模型定义文件，描述数据库有哪些表、哪些字段、表之间的关系 |
| `generated/prisma/` | Prisma 自动生成的 TypeScript 类型文件，不要手动修改，执行 `npx prisma generate` 后自动更新 |

#### 🌍 前端

| 文件/文件夹 | 作用 |
|---|---|
| `public/index.html` | 职位看板的前端页面，纯 HTML+CSS+JS，通过 fetch 调用 server.js 的 API 展示数据 |

---

## 二、按企业标准，还缺哪些文件

企业项目通常分这几个层次：

```
企业项目标准结构
├── 配置层       → 环境变量、数据库配置
├── 路由层       → 定义 URL 路径
├── 控制器层     → 处理请求和响应
├── 服务层       → 业务逻辑
├── 数据访问层   → 数据库操作（你已经有 userRepository.js）
├── 中间件层     → 身份验证、日志、错误处理
├── 工具层       → 公共函数
└── 测试层       → 单元测试、集成测试
```

### 🔴 现在缺少的关键文件

#### 1. 路由拆分（现在全塞在 server.js 里了）

```
routes/
  ├── user.routes.js       # 用户相关路由 GET/POST/PUT/DELETE /api/users
  ├── job.routes.js        # 职位相关路由
  └── order.routes.js      # 订单相关路由
```

**为什么需要**：现在 server.js 又管启动服务器、又管路由、又管业务逻辑，文件会越来越长。企业项目每类接口单独一个文件，清晰好维护。

---

#### 2. 控制器层（处理请求/响应的逻辑）

```
controllers/
  ├── user.controller.js   # 处理用户请求，调用 service，返回 JSON
  ├── job.controller.js    # 处理职位请求
  └── order.controller.js  # 处理订单请求
```

**为什么需要**：把"收到请求→处理→返回响应"这个动作单独抽出来，和业务逻辑分离。

---

#### 3. 服务层（业务逻辑）

```
services/
  ├── user.service.js      # 用户业务逻辑（注册、登录、权限检查）
  ├── job.service.js       # 职位业务逻辑（搜索、推荐）
  └── order.service.js     # 订单业务逻辑（下单、退款、状态流转）
```

**为什么需要**：业务逻辑不应该写在 controller 里。service 层专门处理"怎么做"，controller 只管"收请求、返结果"。

---

#### 4. 中间件（Middleware）

```
middleware/
  ├── auth.middleware.js     # JWT 身份验证，检查用户是否登录
  ├── validate.middleware.js # 请求参数校验（字段不能为空、格式要正确）
  ├── logger.middleware.js   # 记录每次请求的日志
  └── error.middleware.js    # 全局错误处理，统一返回错误格式
```

**为什么需要**：现在接口没有任何验证，任何人都能调用。生产环境必须有身份验证。

---

#### 5. 数据仓库层补全

```
repositories/
  ├── user.repository.js     # 你已经有了（叫 userRepository.js）
  ├── job.repository.js      # 职位的增删改查
  ├── order.repository.js    # 订单的增删改查
  └── product.repository.js  # 商品的增删改查
```

---

#### 6. 工具函数

```
utils/
  ├── response.util.js    # 统一 API 响应格式 { code, message, data }
  ├── password.util.js    # 密码加密/验证（bcrypt）
  └── paginate.util.js    # 分页计算工具函数
```

---

#### 7. 环境配置

```
config/
  └── database.config.js  # 数据库连接配置，根据 NODE_ENV 自动切换开发/生产配置
```

---

#### 8. 测试文件

```
tests/
  ├── user.test.js        # 用户接口的单元测试
  ├── job.test.js         # 职位接口的单元测试
  └── order.test.js       # 下单流程的集成测试
```

---

#### 9. 项目说明

```
README.md               # 项目介绍、如何启动、API 文档
.env.example            # .env 的模板（不包含真实密码，提交到 Git 给别人参考）
```

---

## 三、企业标准的完整目录结构

```
node-app/
│
├── config/
│   └── database.config.js       # 数据库配置
│
├── controllers/
│   ├── user.controller.js       # 用户控制器
│   ├── job.controller.js        # 职位控制器
│   └── order.controller.js      # 订单控制器
│
├── middleware/
│   ├── auth.middleware.js       # 身份验证
│   ├── validate.middleware.js   # 参数校验
│   ├── logger.middleware.js     # 请求日志
│   └── error.middleware.js      # 错误处理
│
├── repositories/
│   ├── user.repository.js       # 用户数据操作
│   ├── job.repository.js        # 职位数据操作
│   ├── order.repository.js      # 订单数据操作
│   └── product.repository.js   # 商品数据操作
│
├── routes/
│   ├── user.routes.js           # 用户路由
│   ├── job.routes.js            # 职位路由
│   └── order.routes.js          # 订单路由
│
├── services/
│   ├── user.service.js          # 用户业务逻辑
│   ├── job.service.js           # 职位业务逻辑
│   └── order.service.js         # 订单业务逻辑
│
├── utils/
│   ├── response.util.js         # 统一响应格式
│   ├── password.util.js         # 密码加密
│   └── paginate.util.js         # 分页工具
│
├── tests/
│   ├── user.test.js
│   ├── job.test.js
│   └── order.test.js
│
├── prisma/
│   └── schema.prisma
│
├── public/
│   └── index.html
│
├── db.js                        # 连接池（你已经有）
├── transaction.js               # 事务工具（你已经有）
├── server.js                    # 启动入口（你已经有）
├── .env                         # 环境变量（你已经有）
├── .env.example                 # 环境变量模板（缺）
├── .gitignore                   # Git 忽略（你已经有）
├── package.json                 # 项目配置（你已经有）
└── README.md                    # 项目文档（缺）
```

---

## 四、一句话总结

| 层 | 职责 | 比喻 |
|---|---|---|
| routes | 接收 URL 请求，分发给对应 controller | 前台接待 |
| controller | 处理请求参数，调用 service，返回结果 | 项目经理 |
| service | 核心业务逻辑 | 工程师 |
| repository | 数据库增删改查 | 仓库管理员 |
| middleware | 请求前的检查（登录了吗？参数对吗？） | 安保 |
| utils | 公共工具函数 | 工具箱 |

---

> 你目前的项目对于学习阶段完全足够。企业项目之所以这样拆分，是因为团队协作、代码复用、后期维护的需要。代码量越大，分层越重要。
