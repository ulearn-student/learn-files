# 企业级网站开发 · 第一部分知识点详解
## 账号密码登录 — 每一步技术原理

---

# 一、前端：表单验证

## 1.1 为什么要在前端做验证？

用户填完表单点击登录时，数据会发送到服务器。
如果手机号只填了8位、密码是空的，这个请求发出去也是无效的，白白浪费一次网络请求。

**前端验证的作用：**
- 在数据发出去之前就拦截，立刻给用户提示
- 减少无效请求，节省服务器资源
- 用户体验更好（不用等待网络往返再告诉你填错了）

> ⚠️ **前端验证是"体验优化"，不是"安全保障"**
> 懂技术的人可以绕过前端，直接用工具向服务器发送任意数据。
> 所以后端必须再做一次验证，这两道验证缺一不可。

---

## 1.2 正则表达式是什么？

正则表达式是一种**字符串匹配规则**，可以描述出某种格式，然后用来检验一个字符串是否符合这个格式。

**生活中的类比：**
就像身份证的格式规则：
- 必须是18位
- 前17位是数字
- 最后一位是数字或 X

正则表达式就是把这种规则写成计算机能看懂的语法。

**常用符号含义：**

| 符号 | 含义 | 举例 |
|------|------|------|
| `^` | 字符串开头 | `^1` 表示必须以1开头 |
| `$` | 字符串结尾 | 配合^确保整体长度 |
| `\d` | 任意一个数字（0-9） | `\d{4}` 表示4位数字 |
| `[3-9]` | 括号内的字符之一 | 3到9中的一个 |
| `{9}` | 恰好重复几次 | `\d{9}` 9位数字 |
| `+` | 出现一次或多次 | `\d+` 一个或多个数字 |
| `*` | 出现零次或多次 | 可有可无 |
| `?` | 出现零次或一次 | 可选 |

**手机号正则拆解：`/^1[3-9]\d{9}$/`**
```
^      → 从头开始匹配
1      → 第1位必须是"1"（国内手机号固定）
[3-9]  → 第2位是3~9其中一个（排除早期停用号段）
\d{9}  → 后面还有9位任意数字
$      → 到此为止，不能多也不能少
合计：1位 + 1位 + 9位 = 11位
```

---

# 二、网络传输：HTTP 协议

## 2.1 什么是 HTTP？

HTTP（HyperText Transfer Protocol，超文本传输协议）是浏览器和服务器之间**沟通的语言规范**。

就像寄快递有规定格式（寄件人、收件人、物品描述），HTTP 也规定了请求和响应的格式。

**完整通信过程：**
```
① 用户点击登录按钮
② 浏览器按照 HTTP 格式打包数据（账号+密码）
③ 通过网络发送给服务器
④ 服务器按照 HTTP 格式返回结果（Token）
⑤ 浏览器解析响应，完成登录
```

---

## 2.2 GET 和 POST 的区别

HTTP 有多种请求方法，登录用的是 POST：

| 方法 | 用途 | 数据位置 | 特点 |
|------|------|--------|------|
| **GET** | 获取数据 | URL 参数 | 数据在地址栏可见，不适合传密码 |
| **POST** | 提交数据 | 请求体 | 数据在请求体，相对安全 |
| PUT | 修改数据 | 请求体 | 全量更新 |
| DELETE | 删除数据 | URL 参数 | 删除资源 |

**为什么登录用 POST 不用 GET？**
GET 请求的数据会拼接在 URL 上，例如：
```
https://example.com/login?phone=13800&password=abc123
```
密码直接暴露在地址栏，会被浏览器历史记录、服务器日志、网络代理等多个地方记录下来，极度不安全。

POST 的数据放在**请求体**里，不在 URL 上显示。

---

## 2.3 HTTP 请求的结构

一次 HTTP 请求由三部分组成：

**① 请求行（Request Line）**
说明这次请求的目的地和方式：
```
POST /api/auth/login HTTP/1.1
↑      ↑                ↑
方法  路径（接口地址）  协议版本
```

**② 请求头（Request Headers）**
携带附加信息，每行一个字段：
```
Host: your-api.com              → 目标服务器域名
Content-Type: application/json  → 告诉服务器请求体是 JSON 格式
Authorization: Bearer eyJ...    → 身份凭证（登录后的请求带上）
```

**③ 请求体（Request Body）**
POST 独有，存放提交的数据，登录时内容是：
```json
{
  "phone": "13800138000",
  "password": "abc12345"
}
```

---

## 2.4 HTTP 状态码

服务器返回响应时，会带一个3位数字的状态码，告诉前端这次请求的结果：

| 状态码 | 含义 | 登录场景举例 |
|--------|------|------------|
| **200** | 成功 | 登录成功，返回 Token |
| **400** | 请求参数有问题 | 手机号格式不对 |
| **401** | 未授权（未登录或凭证无效） | 密码错误 / Token 过期 |
| **403** | 有身份但没权限 | 账号被封禁 |
| **404** | 资源不存在 | 接口地址写错了 |
| **500** | 服务器内部错误 | 数据库崩了 / 代码 bug |

> 前端根据状态码做不同处理，比如收到 401 就跳转登录页，收到 500 就提示"服务器异常"。

---

## 2.5 HTTPS：传输安全的根本保障

**HTTP 的问题：**
数据在网络上传输时是明文的，中间的路由器、WiFi 热点都能看到内容。

**HTTPS = HTTP + TLS 加密：**
```
普通 HTTP：
浏览器 → [账号: 138... 密码: abc123（明文）] → 服务器
途中任何节点都能看到

HTTPS：
浏览器 → [#@!%*&...（加密后的乱码）] → 服务器
只有服务器能解密，途中看不懂
```

HTTPS 使用 **TLS 协议**（传输层安全），在发送数据前先和服务器进行"握手"，协商一个只有双方知道的密钥，之后所有数据都用这个密钥加密传输。

**在实际开发中：**
- 开发阶段（本机）：用 HTTP 也可以
- 上线后（生产环境）：必须配置 HTTPS 证书，否则浏览器会警告

---

# 三、前端工具：Axios

## 3.1 Axios 是什么？

Axios 是一个专门用来发送 HTTP 请求的 JavaScript 库。

**浏览器本身能发请求，为什么还要用 Axios？**

浏览器原生提供了 `fetch` API 可以发请求，但 Axios 在它基础上做了很多封装，让开发更方便：

| 功能 | 原生 fetch | Axios |
|------|------------|-------|
| 自动解析 JSON | 需要手动 `.json()` | 自动解析 |
| 请求超时设置 | 麻烦，需要额外处理 | 一行配置 |
| 统一拦截处理 | 不支持 | 支持拦截器 |
| 错误处理 | 需要判断 ok 属性 | 非200自动进 catch |
| 取消请求 | 麻烦 | 简单 |

---

## 3.2 拦截器是什么？

拦截器分两种：**请求拦截器**和**响应拦截器**，就像流水线上的两个检查站。

**请求拦截器（发出去之前）：**
```
代码发出请求
    ↓
请求拦截器（自动加上 Token）
    ↓
真正发出去
```

**响应拦截器（收到之后）：**
```
服务器返回数据
    ↓
响应拦截器（检查状态码，401就跳登录页）
    ↓
数据交给业务代码处理
```

**好处：**
所有请求统一加 Token，所有响应统一处理错误，不需要每个接口都写一遍，维护起来非常方便。

---

## 3.3 Token 为什么存在 localStorage？

登录成功后，前端要把 Token 保存起来，下次请求时带上。存储位置有几种选择：

| 存储位置 | 特点 | 安全性 | 推荐度 |
|---------|------|--------|--------|
| **localStorage** | 关掉浏览器不消失，手动清除才没 | 存在 XSS 风险 | ⭐⭐⭐ 最常用 |
| **sessionStorage** | 关掉标签页就消失 | 同 localStorage | ⭐⭐ 短期使用 |
| **httpOnly Cookie** | JS 无法读取，防 XSS | 最安全 | ⭐⭐⭐⭐ 推荐 |
| 内存变量 | 刷新页面就没了 | 安全 | ⭐ 不实用 |

> 初学阶段用 localStorage 最简单。生产级项目推荐 httpOnly Cookie，但配置稍复杂，后面讲到安全章节会详细说。

---

# 四、后端：Express 框架

## 4.1 什么是服务器？

服务器就是一台一直运行的电脑，专门等待并处理客户端（浏览器/App）发来的请求。

**普通电脑和服务器的区别：**
- 普通电脑：你用的时候开机，不用就关机
- 服务器：24小时不间断运行，随时响应请求

在开发阶段，你自己的电脑就可以充当服务器（本地服务器）。上线后才需要把代码部署到云服务器上。

---

## 4.2 Express 是什么？

Express 是 Node.js 上最流行的 Web 框架，帮你快速搭建 HTTP 服务器。

**没有框架时的问题：**
如果直接用 Node.js 原生写服务器，需要手动解析请求地址、手动读取请求体、手动设置响应头……非常繁琐。

**Express 做了什么：**
把这些重复的工作封装好，让你专注于写业务逻辑。

---

## 4.3 路由是什么？

路由（Route）定义了"什么地址 + 什么方法 = 执行什么逻辑"。

**生活类比：**
就像公司前台的接待规则：
- 来访者说"我要见销售" → 引导到销售部
- 来访者说"我要送快递" → 引导到收发室
- 来访者说"我要面试" → 引导到 HR 部门

路由就是这张规则表：

```
POST /api/auth/login    → 执行登录逻辑
POST /api/auth/register → 执行注册逻辑
GET  /api/user/profile  → 执行获取用户信息逻辑
PUT  /api/user/update   → 执行更新用户信息逻辑
```

---

## 4.4 中间件是什么？

中间件（Middleware）是在"收到请求"和"处理请求"之间插入的函数，可以做一些公共的事情。

**生活类比：**
进办公室之前要刷门禁卡（安全验证中间件）。
如果你没有门禁卡，直接被拒之门外，不会进到具体的办公区域。

**登录验证中间件的作用：**
```
收到请求
    ↓
验证 Token 中间件（有 Token 且有效 → 放行，否则 → 401）
    ↓
实际的接口处理函数
```

这样每个需要登录才能访问的接口，都挂上这个中间件，就不需要在每个接口里写一遍验证逻辑。

---

# 五、密码安全：bcrypt

## 5.1 为什么密码不能明文存储？

假设你的数据库被黑客拿到了（这在历史上发生过很多次），如果密码是明文：
- 所有用户密码全部暴露
- 大多数人在不同网站用同一个密码，关联账号一起沦陷
- 用户永远无法知道密码被盗，无法及时修改

**哈希存储的好处：**
即使数据库被盗，黑客拿到的是一堆无法还原的哈希值，无法知道原始密码。

---

## 5.2 哈希算法的原理

哈希（Hash）是一种**单向函数**，把任意长度的输入变成固定长度的输出，且**不可逆**。

**不可逆的原因：**
哈希运算是多对一的，理论上无数个输入可能对应同一个输出（但概率极低）。正是因为这种"多对一"的特性，无法从输出推断唯一的输入。

**生活类比：**
把一份文件放进碎纸机。碎出来的纸屑是哈希值，原始文件是密码。你可以验证"这堆碎纸屑是不是这份文件碎的"，但无法从碎纸屑还原出文件。

---

## 5.3 什么是盐值（Salt）？

**问题：**
如果两个用户都用了密码 "abc123"，他们的哈希值会完全相同。黑客可以预先计算出常见密码的哈希值，做成一张对照表（彩虹表），看到哈希值就能查到原始密码。

**盐值的作用：**
在哈希之前，给密码加入一段随机字符串（盐值），使得：
```
用户A 的密码 "abc123" + 盐值 "xT9k" → 哈希结果 A
用户B 的密码 "abc123" + 盐值 "mQ7p" → 哈希结果 B
```
两个相同的密码，因为盐值不同，哈希结果完全不同，彩虹表失效。

**bcrypt 的特别之处：**
- 盐值自动随机生成，嵌入哈希结果里，验证时自动提取
- saltRounds 参数让运算重复多次，越大越慢，暴力破解成本极高
- 即使是同一个密码，每次 bcrypt 的结果都不一样（盐值每次不同）

---

# 六、登录状态：JWT

## 6.1 为什么需要保持登录状态？

**HTTP 协议是无状态的：**
每一次 HTTP 请求之间完全独立，服务器不会记住上一次请求是谁发的。

这就意味着：你登录之后发的第二个请求，服务器不知道你已经登录了。

**解决方法：**
登录成功后，给用户一个"凭证"（Token），以后每次请求带上这个凭证，服务器验证凭证后就知道你是谁了。

---

## 6.2 Session 和 JWT 的区别

这是保持登录状态的两种主流方案：

**Session 方式（有状态）：**
```
① 登录成功 → 服务器在内存里记录 "SessionID = {userId: 1}"
② 把 SessionID 发给浏览器（存在 Cookie 里）
③ 下次请求带上 SessionID
④ 服务器查内存，找到对应的用户信息
```
缺点：状态存在服务器内存，服务器重启就丢失，多台服务器无法共享。

**JWT 方式（无状态）：**
```
① 登录成功 → 服务器生成 Token（包含用户信息），什么都不存
② 把 Token 发给前端（存在 localStorage 里）
③ 下次请求在请求头带上 Token
④ 服务器验证 Token 的签名和有效期，直接解析出用户信息
```
优点：服务器不存任何状态，扩展方便，多台服务器都能验证。

---

## 6.3 JWT 的三部分结构

JWT 的格式是：`Header.Payload.Signature`，三部分用点号分隔。

**第一部分：Header（头部）**
说明这个 Token 使用的加密算法：
```json
{
  "alg": "HS256",  // 算法名称（HMAC-SHA256）
  "typ": "JWT"     // 类型
}
```
经过 base64 编码后变成第一段字符串。

**第二部分：Payload（载荷）**
存放实际信息：
```json
{
  "userId": 1,         // 用户 ID
  "role": "admin",     // 角色
  "iat": 1700000000,   // 签发时间（issued at）
  "exp": 1700604800    // 过期时间（expiration）
}
```
经过 base64 编码后变成第二段字符串。

> ⚠️ **Payload 不是加密！** base64 是编码，任何人都能解码看到内容。
> 所以 Payload 里只放不敏感的数据，绝不放密码、身份证号等。

**第三部分：Signature（签名）**
用服务器保存的密钥，对前两部分进行 HMAC 签名：
```
Signature = HMAC-SHA256(
  base64(Header) + "." + base64(Payload),
  SECRET_KEY  ← 只有服务器知道的密钥
)
```

**签名的作用：**
如果有人修改了 Payload（比如把 userId 改成别人的），签名就会对不上，服务器验证失败，请求被拒绝。

所以即使 Payload 是可见的，也无法伪造一个有效的 Token。

---

## 6.4 Token 过期机制

Payload 里的 `exp` 字段存储过期时间戳。
服务器验证 Token 时，会对比当前时间和 `exp`：
- 当前时间 < exp → Token 有效
- 当前时间 ≥ exp → Token 过期，返回 401

**为什么要设置过期时间？**
Token 一旦生成就无法撤销（除非维护黑名单，增加复杂度）。
设置过期时间，就算 Token 被盗，超过有效期后自动失效，降低风险。

---

# 七、数据库：MySQL

## 7.1 关系型数据库是什么？

数据库是专门用来存储和管理数据的软件。MySQL 是**关系型数据库**，把数据组织成**二维表格**（像 Excel 表格一样）。

```
users 表（用户表）：
┌────┬─────────────┬────────────────────┬──────────┐
│ id │    phone    │      password      │ nickname │
├────┼─────────────┼────────────────────┼──────────┤
│  1 │ 13800138001 │ $2b$10$xyz...（哈希）│ 小明     │
│  2 │ 13900139002 │ $2b$10$abc...（哈希）│ 小红     │
│  3 │ 13700137003 │ $2b$10$def...（哈希）│ 小刚     │
└────┴─────────────┴────────────────────┴──────────┘
```

- 每一行叫一条**记录**（一个用户的数据）
- 每一列叫一个**字段**（数据的某个属性）

---

## 7.2 SQL 语句基础

SQL 是操作数据库的语言，登录流程中主要用到两种：

**查询语句（SELECT）：**
```
SELECT  id, phone, password  → 取出这几个字段
FROM    users                → 从 users 表里
WHERE   phone = '138...'     → 条件：手机号等于...
```

**更新语句（UPDATE）：**
```
UPDATE  users               → 修改 users 表
SET     last_login = NOW()  → 把 last_login 字段设置成当前时间
WHERE   id = 1              → 条件：只修改 id=1 的那一行
```

---

## 7.3 什么是 SQL 注入？为什么危险？

**SQL 注入**是最常见的数据库攻击方式。

**原理：**
如果把用户输入的内容直接拼进 SQL 语句，黑客可以输入特殊内容改变 SQL 的含义。

**举例：**
你的 SQL 是：`SELECT * FROM users WHERE phone = '用户输入的手机号'`

如果用户输入的不是手机号，而是：`' OR '1'='1`

拼进去后 SQL 变成：
```sql
SELECT * FROM users WHERE phone = '' OR '1'='1'
```
`'1'='1'` 永远成立，这个查询会返回所有用户数据！

**防护方法：参数化查询**
不把用户输入直接拼进 SQL，而是用 `?` 占位符，让数据库驱动自动做安全处理：
```sql
SELECT * FROM users WHERE phone = ?
```
驱动会对输入内容自动转义，确保它只能作为普通字符串，无法改变 SQL 结构。

---

## 7.4 索引是什么？为什么要建索引？

**没有索引时的查询：**
在1000万条数据里找手机号为 "13800138001" 的用户，数据库需要逐行扫描，从第1行扫到第1000万行，找到为止。数据越多越慢。

**有索引时的查询：**
索引相当于书的"目录"。数据库提前为手机号字段建立了一棵 B+ 树（一种有序的数据结构），查找时可以快速定位，不需要逐行扫描。1000万条数据中查找，只需几十毫秒。

**什么字段应该建索引？**
- 经常出现在 WHERE 条件里的字段（比如手机号、邮箱）
- 唯一约束的字段（主键自动有索引）
- 不适合：很少查询的字段、频繁写入的字段（索引会拖慢写入速度）

---

## 7.5 字段类型怎么选？

| 类型 | 适合存什么 | 说明 |
|------|---------|------|
| `INT` | 整数（ID、数量） | UNSIGNED 表示只存正整数，范围翻倍 |
| `VARCHAR(n)` | 可变长度字符串 | n 是最大长度，手机号用11 |
| `CHAR(n)` | 固定长度字符串 | 每行占固定空间，适合固定长度的数据 |
| `DATETIME` | 日期时间 | 格式：2024-01-01 12:00:00 |
| `ENUM` | 固定的几个选项 | 比如状态只有 active/banned，用 ENUM |
| `TEXT` | 长文本 | 文章内容、评论等，不限长度 |

---

# 八、整体知识串联

## 完整登录流程每一步的技术对应

```
用户填写手机号和密码
        ↓
【正则表达式】验证格式是否合法
        ↓
【Axios】把数据打包成 HTTP POST 请求发出去
        ↓
【HTTPS / TLS】传输过程中加密，防止数据被截获
        ↓
【Express 路由】服务器收到请求，分发到登录处理函数
        ↓
【中间件】格式再次验证
        ↓
【MySQL + 参数化查询】从数据库查找该手机号的用户
        ↓
【bcrypt】对比输入密码和数据库里的哈希值
        ↓
【JWT】生成 Token，包含用户 ID 和角色
        ↓
【HTTP 响应】把 Token 和用户信息返回给前端
        ↓
【localStorage】前端把 Token 存起来
        ↓
【Axios 拦截器】后续每次请求自动带上 Token
        ↓
【JWT 验证中间件】服务器验证 Token 有效性
```

## 各技术的职责分工

| 技术 | 职责 | 位置 |
|------|------|------|
| 正则表达式 | 格式验证 | 前端 |
| Axios | HTTP 请求发送 | 前端 |
| Axios 拦截器 | 自动带 Token / 处理错误 | 前端 |
| localStorage | Token 存储 | 前端 |
| Express | 搭建 HTTP 服务器 | 后端 |
| 路由 | 请求分发 | 后端 |
| 中间件 | 公共逻辑处理（验证） | 后端 |
| bcrypt | 密码哈希对比 | 后端 |
| JWT | 生成/验证 Token | 后端 |
| MySQL | 存储用户数据 | 数据库 |
| 参数化查询 | 防 SQL 注入 | 数据库操作层 |
| HTTPS | 传输加密 | 网络层 |
| 索引 | 加速数据库查询 | 数据库 |

---

> 第二部分：手机验证码登录
> 新增知识：Redis（内存数据库）、阿里云短信 API、验证码防刷机制

